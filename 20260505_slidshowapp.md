これからアプリケーション開発をするためのディスカッションを行います。
あなたは私の開発のサポートをしてください。

以下の要件に合わせて適切な言語とFWを選択し、このプロジェクトを進めてください。
現状、Geminiによる提案によるとElectron + React + PixiJSを提案されています。

この開発はLLMによる自動コーディングで実装・メンテナンスする想定として。

# 要件
## 開発アプリケーション
- 動的ビジュアルエフェクトをオーバレイ描画できる、画像ビューア

## OS
- Windows 11以上

## 基本機能
- ブランクスペースはユーザが選択した色で塗り潰せる
- 複数表示した各画像は自動切り替え（スライドショー表示）できること
- 各表示画像の上から動的エフェクトを表示する動的エフェクト機能を備える
- 他OSに用意に対応できるものだとなお良い（Electronなど）
- PCリソース負荷はそこまで抑えなくても良い（ミドルクラスのグラフィックボード前提）

## 画像・フォルダ選択
- ローカルで用意された画像を横1から7列など複数同時に表示できること
- 表示画像は指定フォルダを参照する
- 表示順はランダム表示を可能とする
- 各画像は異なるフォルダを選択できるものとする
- フォルダ選択時はドラッグアンドドロップで指定可能とする
- フルスクリーン表示可能
- アプリケーション本体大枠（マスタウィンドウ）をフルスクリーンとし、その内部で画像を分割表示する
- 画像をマスタウィンドウにドラッグするたびに自動的に画像を縦に分割し、自動的に均等分割の横並びで表示していく
- 最大描画列数は15列、最大描画行数は15行までとする

## 動的エフェクト機能
- 色調オーバレイ機能
    * ユーザが指定した色を画像の上から静的オーバレイ表示する
- ビネットエフェクト機能
    * ピンク色の周辺減光（ビネット）を表示中画像の上からオーバレイ描画する(アセット1)
    * ビネットエフェクトはユーザが選択した透明度で描画可能
    * 「動的ビネットエフェクト機能」は例えば1秒間かけて指定した透明度（40% -> 70%）の変更しながら描画する（その後初期値に即時戻り繰り返す）
- ブラーエフェクト機能
    * 画像の上からぼかし（ブラー）を適用する
    * ブラーは画像のみにかけるか、他の追加エフェクトすべてにかけるか選択可能とする
    * ブラーは指定した時間をかけて徐々に強度を増加できるようにする（最大3600秒）
- 動的アセットオーバレイ機能
    * ユーザが用意した透過画像（アセット）を画像の上から描画する
    * 選択されたアセットは画像中のランダムな位置に表示し、徐々に透過しながら上に上昇するような描画を実現する（ハートマークなど）
- タイマー機能
    * マスタウィンドウ（大枠）に1つのみ表示する
    * 表示位置は上中下、左中右の9箇所から選択できるようにする
    * タイマーはプログレスバー表示する

## プロファイル保存機能
- 描画中の分割状態・選択フォルダ・アセットオーバレイ状態などを保存し、jsonでエクスポート・インポート可能とする（ほかユーザとの共有・バックアップ目的）

## Tasks
- 画像の表示フィットオプションを選択できるようにする。（画像の高さに合わせる、横幅似合わせる、大きい方に合わせる）
- 選択中のフォルダのパスを表示するようにする
- カラムを0,0ではなく1から始まるようにする
- 各エフェクトの変化時間の単位をすべて"秒"に統一する
- ビネットエフェクトとブラーエフェクトの開始タイミングを常に同期するように変更
- ブラーエフェクトを指定したエリアにのみかけられるように機能を追加
    * 1. ビネットエフェクトと同様に周囲にのみかける
    * 2. ユーザが自由選択した円形エリアにのみかける。円は高さ・幅を指定して楕円形に可能、サイズの変更も可能とする。
- アセットエフェクトのアセット自体にカラーオーバレイ（ビネットエフェクトと同じカラーテーブル）をかけられるようにする

