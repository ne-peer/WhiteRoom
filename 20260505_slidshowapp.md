これからアプリケーション開発をするためのディスカッションを行います。
あなたは私の開発のサポートをしてください。

以下の要件に合わせて適切な言語とFWを選択し、このプロジェクトを進めてください。
現状、Geminiによる提案によるとElectron + React + PixiJSを提案されています。

この開発はLLMによる自動コーディングで実装・メンテナンスする想定として。

# 要件
## 開発アプリケーション
- 動的ビジュアルエフェクトをオーバレイ描画できる、画像ビューア

## OS
- Windows 11以上

## 基本機能
- ブランクスペースはユーザが選択した色で塗り潰せる
- 複数表示した各画像は自動切り替え（スライドショー表示）できること
- 各表示画像の上から動的エフェクトを表示する動的エフェクト機能を備える
- 他OSに用意に対応できるものだとなお良い（Electronなど）
- PCリソース負荷はそこまで抑えなくても良い（ミドルクラスのグラフィックボード前提）

## 画像・フォルダ選択
- ローカルで用意された画像を横1から7列など複数同時に表示できること
- 表示画像は指定フォルダを参照する
- 表示順はランダム表示を可能とする
- 各画像は異なるフォルダを選択できるものとする
- フォルダ選択時はドラッグアンドドロップで指定可能とする
- フルスクリーン表示可能
- アプリケーション本体大枠（マスタウィンドウ）をフルスクリーンとし、その内部で画像を分割表示する
- 画像をマスタウィンドウにドラッグするたびに自動的に画像を縦に分割し、自動的に均等分割の横並びで表示していく
- 最大描画列数は15列、最大描画行数は15行までとする

## 動的エフェクト機能
- 色調オーバレイ機能
    * ユーザが指定した色を画像の上から静的オーバレイ表示する
- ビネットエフェクト機能
    * ピンク色の周辺減光（ビネット）を表示中画像の上からオーバレイ描画する(アセット1)
    * ビネットエフェクトはユーザが選択した透明度で描画可能
    * 「動的ビネットエフェクト機能」は例えば1秒間かけて指定した透明度（40% -> 70%）の変更しながら描画する（その後初期値に即時戻り繰り返す）
- ブラーエフェクト機能
    * 画像の上からぼかし（ブラー）を適用する
    * ブラーは画像のみにかけるか、他の追加エフェクトすべてにかけるか選択可能とする
    * ブラーは指定した時間をかけて徐々に強度を増加できるようにする（最大3600秒）
- 動的アセットオーバレイ機能
    * ユーザが用意した透過画像（アセット）を画像の上から描画する
    * 選択されたアセットは画像中のランダムな位置に表示し、徐々に透過しながら上に上昇するような描画を実現する（ハートマークなど）
- タイマー機能
    * マスタウィンドウ（大枠）に1つのみ表示する
    * 表示位置は上中下、左中右の9箇所から選択できるようにする
    * タイマーはプログレスバー表示する

## プロファイル保存機能
- 描画中の分割状態・選択フォルダ・アセットオーバレイ状態などを保存し、jsonでエクスポート・インポート可能とする（ほかユーザとの共有・バックアップ目的）

## Tasks
### Done
- 画像の表示フィットオプションを選択できるようにする。（画像の高さに合わせる、横幅似合わせる、大きい方に合わせる）
- 選択中のフォルダのパスを表示するようにする
- カラムを0,0ではなく1から始まるようにする
- 各エフェクトの変化時間の単位をすべて"秒"に統一する
- ビネットエフェクトとブラーエフェクトの開始タイミングを常に同期するように変更
- ビネットエフェクト・ブラーエフェクトを同一設定で全カラムに一斉反映する機能を追加
- スライドショーの開始タイミングを全体カラムで一括ランダムで再開する機能を追加
- スライドショーで画像切り替え時に一周白画像が描画される問題を修正
- アセットエフェクトのアセット自体にカラーオーバレイ（ビネットエフェクトと同じカラーテーブル）をかけられるようにする
- デフォルトのスライドショーの時間を3秒に変更
- 選択中のカラムで「すべてのカラムにフォルダを反映」ボタンを追加。この機能で全カラムに同じフォルダを一括参照するように設定する
- アセットエフェクトを同一設定ですべてのタブに一括反映できる機能を追加
- アセットエフェクトの上昇速度を{2px, 4px, 6px}と3段階をデフォルト設定とし、この設定値からユーザが任意で速度を追加・減速できるように変更
- ドロップダウンUIの背景色と文字色が両方白になっていてオンフォーカス時以外に読めないので可読性を高めるよう修正
- グリッドタブでスライドショーの設定もすべてのカラムに反映できるボタンを追加
- 各グリッド上に画像をD&Dした際に、自動的にそのグリッドで親フォルダのパスを読み込むように変更
- フルスクリーン表示時に右の操作UIを非表示にする方法がないためボタンを追加
- フルスクリーン時に画面上部のナビゲーションバーを自動的に非表示にする。ウィンドウ表示時にもバーを非表示にできるボタンを追加する。
- スライドショーの画像切り替え時のトランジションエフェクトをいくつか用意し、グリッドタブに追加
- 「動的アセットオーバレイ」の機能名を「アセットエフェクト」に変更
- アセットエフェクトの上昇速度が現在は「2px, 4px, 6px」に分離しているが、この選択肢自体を削除しデフォルトで「2px, 4px, 6pxの上昇速度のどれかからランダムでアセットを描画する」ように修正
- ブラーエフェクトの中心位置選択の位置がずれており、クリックした場所と異なる場所が選択されるバグを修正
- D&Dが動作していないため修正（エラーは先程と同じ内容）
- ブラーエフェクトは既存の「全体に均等にかける」機能はのこしつつ、中心位置から放射線状に徐々にブラーが強くなるように（ビネットのように）ブラーを反映できるように機能を追加
- ブラーエフェクトの「徐々に増加」有効時、指定した所要時間の途中で一度強度がリセットされているバグの修正
- ブラーエフェクトとビネットエフェクトの開始タイミングを同期する機能を追加
- 次の実装が動作していないため修正：エフェクトタブの「開始タイミングをランダムに再開」ボタンを押すと、各カラムのビネットエフェクト＆ブラーエフェクトの開始タイミングをランダムに開始するように修正。ただし各カラムごとのビネットとブラーエフェクトの開始タイミングの同期はズラさないこと。
    - ボタンを押しても各カラムがランダムにならない
- アセットエフェクトの「フォルダからランダムに描画」が実現できていない。フォルダを選択できない。
- アセットの表示開始位置をユーザが調整できる用になっていないため修正：アセットエフェクトのアセット開始位置を下部固定ではなく、画像底辺からの高さ上限70%までのランダムな位置から発生するように機能を追加。発生位置の高さ上限はユーザが指定できるように実装。
- アセットはファイル単体またはフォルダを指定し、フォルダ選択した場合はフォルダ内の画像からランダムで表示する機能を追加
- エコーエフェクト機能を実装
    - ブラー・ビネットと同列の機能として「エコーエフェクト」機能を実装する
    - エコーエフェクトは表示中の画像を拡大しつつ徐々に透明度を100に近づけていくエフェクト
    - ビネット機能と同様にエフェクトをかける時間をユーザが指定できるようにし、指定した時間で繰り返し実行する
    - 「一斉反映」セクションの各機能にエコーエフェクトも追加する。「ビネット・ブラー設定を全カラムへ反映」ボタンは省略して「エフェクトを全カラムへ反映」ラベルとする。
- ブリージングエフェクトを実装(breathing effect)
    * ビネット・ブラーエフェクトなどと同等の機能、手ブレした映像のような表現
    * 表示中の画像をゆっくり上下＋左右の2方向に非常にゆっくり動きながら、ある程度のしきい値まで移動したら跳ね返ってまた別の方向に移動する
    * しきい値は上下左右で0-40pxで選択可能とする
    * 画像が移動して空いた部分は背景色を表示する
    * 移動速度はユーザが指定できるものとする
    * オプションとして、画像をわずかに拡大・縮小をゆっくり繰り返す追加エフェクトを付与できるものとする
    * 拡大は表示中画像サイズから最大5%、縮小は表示中ファイルの100%以下にならないようにする
    * 他のエフェクトと同等に「エフェクトを全カラムへ反映」ボタンで他のカラムへ反映できるようにする

### TODO
