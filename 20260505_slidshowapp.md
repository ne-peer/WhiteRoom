これからアプリケーション開発をするためのディスカッションを行います。
あなたは私の開発のサポートをしてください。

以下の要件に合わせて適切な言語とFWを選択し、このプロジェクトを進めてください。
現状、Geminiによる提案によるとElectron + React + PixiJSを提案されています。

この開発はLLMによる自動コーディングで実装・メンテナンスする想定として。

# 要件
## 開発アプリケーション
- 動的ビジュアルエフェクトをオーバレイ描画できる、画像ビューア

## OS
- Windows 11以上

## 基本機能
- ブランクスペースはユーザが選択した色で塗り潰せる
- 複数表示した各画像は自動切り替え（スライドショー表示）できること
- 各表示画像の上から動的エフェクトを表示する動的エフェクト機能を備える
- 他OSに用意に対応できるものだとなお良い（Electronなど）
- PCリソース負荷はそこまで抑えなくても良い（ミドルクラスのグラフィックボード前提）

## 画像・フォルダ選択
- ローカルで用意された画像を横1から7列など複数同時に表示できること
- 表示画像は指定フォルダを参照する
- 表示順はランダム表示を可能とする
- 各画像は異なるフォルダを選択できるものとする
- フォルダ選択時はドラッグアンドドロップで指定可能とする
- フルスクリーン表示可能
- アプリケーション本体大枠（マスタウィンドウ）をフルスクリーンとし、その内部で画像を分割表示する
- 画像をマスタウィンドウにドラッグするたびに自動的に画像を縦に分割し、自動的に均等分割の横並びで表示していく
- 最大描画列数は15列、最大描画行数は15行までとする

## 動的エフェクト機能
- 色調オーバレイ機能
    * ユーザが指定した色を画像の上から静的オーバレイ表示する
- ビネットエフェクト機能
    * ピンク色の周辺減光（ビネット）を表示中画像の上からオーバレイ描画する(アセット1)
    * ビネットエフェクトはユーザが選択した透明度で描画可能
    * 「動的ビネットエフェクト機能」は例えば1秒間かけて指定した透明度（40% -> 70%）の変更しながら描画する（その後初期値に即時戻り繰り返す）
- ブラーエフェクト機能
    * 画像の上からぼかし（ブラー）を適用する
    * ブラーは画像のみにかけるか、他の追加エフェクトすべてにかけるか選択可能とする
    * ブラーは指定した時間をかけて徐々に強度を増加できるようにする（最大3600秒）
- 動的アセットオーバレイ機能
    * ユーザが用意した透過画像（アセット）を画像の上から描画する
    * 選択されたアセットは画像中のランダムな位置に表示し、徐々に透過しながら上に上昇するような描画を実現する（ハートマークなど）
- タイマー機能
    * マスタウィンドウ（大枠）に1つのみ表示する
    * 表示位置は上中下、左中右の9箇所から選択できるようにする
    * タイマーはプログレスバー表示する

## プロファイル保存機能
- 描画中の分割状態・選択フォルダ・アセットオーバレイ状態などを保存し、jsonでエクスポート・インポート可能とする（ほかユーザとの共有・バックアップ目的）

## Tasks
### Done
- 画像の表示フィットオプションを選択できるようにする。（画像の高さに合わせる、横幅似合わせる、大きい方に合わせる）
- 選択中のフォルダのパスを表示するようにする
- カラムを0,0ではなく1から始まるようにする
- 各エフェクトの変化時間の単位をすべて"秒"に統一する
- ビネットエフェクトとブラーエフェクトの開始タイミングを常に同期するように変更
- ビネットエフェクト・ブラーエフェクトを同一設定で全カラムに一斉反映する機能を追加
- スライドショーの開始タイミングを全体カラムで一括ランダムで再開する機能を追加
- スライドショーで画像切り替え時に一周白画像が描画される問題を修正
- アセットエフェクトのアセット自体にカラーオーバレイ（ビネットエフェクトと同じカラーテーブル）をかけられるようにする
- デフォルトのスライドショーの時間を3秒に変更
- 選択中のカラムで「すべてのカラムにフォルダを反映」ボタンを追加。この機能で全カラムに同じフォルダを一括参照するように設定する
- アセットエフェクトを同一設定ですべてのタブに一括反映できる機能を追加
- アセットエフェクトの上昇速度を{2px, 4px, 6px}と3段階をデフォルト設定とし、この設定値からユーザが任意で速度を追加・減速できるように変更
- ドロップダウンUIの背景色と文字色が両方白になっていてオンフォーカス時以外に読めないので可読性を高めるよう修正
- グリッドタブでスライドショーの設定もすべてのカラムに反映できるボタンを追加
- 各グリッド上に画像をD&Dした際に、自動的にそのグリッドで親フォルダのパスを読み込むように変更
- フルスクリーン表示時に右の操作UIを非表示にする方法がないためボタンを追加
- フルスクリーン時に画面上部のナビゲーションバーを自動的に非表示にする。ウィンドウ表示時にもバーを非表示にできるボタンを追加する。
- スライドショーの画像切り替え時のトランジションエフェクトをいくつか用意し、グリッドタブに追加
- 「動的アセットオーバレイ」の機能名を「アセットエフェクト」に変更
- アセットエフェクトの上昇速度が現在は「2px, 4px, 6px」に分離しているが、この選択肢自体を削除しデフォルトで「2px, 4px, 6pxの上昇速度のどれかからランダムでアセットを描画する」ように修正
- ブラーエフェクトの中心位置選択の位置がずれており、クリックした場所と異なる場所が選択されるバグを修正
- D&Dが動作していないため修正（エラーは先程と同じ内容）
- ブラーエフェクトは既存の「全体に均等にかける」機能はのこしつつ、中心位置から放射線状に徐々にブラーが強くなるように（ビネットのように）ブラーを反映できるように機能を追加
- ブラーエフェクトの「徐々に増加」有効時、指定した所要時間の途中で一度強度がリセットされているバグの修正
- ブラーエフェクトとビネットエフェクトの開始タイミングを同期する機能を追加
- 次の実装が動作していないため修正：エフェクトタブの「開始タイミングをランダムに再開」ボタンを押すと、各カラムのビネットエフェクト＆ブラーエフェクトの開始タイミングをランダムに開始するように修正。ただし各カラムごとのビネットとブラーエフェクトの開始タイミングの同期はズラさないこと。
    - ボタンを押しても各カラムがランダムにならない
- アセットエフェクトの「フォルダからランダムに描画」が実現できていない。フォルダを選択できない。
- アセットの表示開始位置をユーザが調整できる用になっていないため修正：アセットエフェクトのアセット開始位置を下部固定ではなく、画像底辺からの高さ上限70%までのランダムな位置から発生するように機能を追加。発生位置の高さ上限はユーザが指定できるように実装。
- アセットはファイル単体またはフォルダを指定し、フォルダ選択した場合はフォルダ内の画像からランダムで表示する機能を追加
- エコーエフェクト機能を実装
    - ブラー・ビネットと同列の機能として「エコーエフェクト」機能を実装する
    - エコーエフェクトは表示中の画像を拡大しつつ徐々に透明度を100に近づけていくエフェクト
    - ビネット機能と同様にエフェクトをかける時間をユーザが指定できるようにし、指定した時間で繰り返し実行する
    - 「一斉反映」セクションの各機能にエコーエフェクトも追加する。「ビネット・ブラー設定を全カラムへ反映」ボタンは省略して「エフェクトを全カラムへ反映」ラベルとする。
- ブリージングエフェクトを実装(breathing effect)
    - ビネット・ブラーエフェクトなどと同等の機能、手ブレした映像のような表現
    - 表示中の画像をゆっくり上下＋左右の2方向に非常にゆっくり動きながら、ある程度のしきい値まで移動したら跳ね返ってまた別の方向に移動する
    - しきい値は上下左右で0-40pxで選択可能とする
    - 画像が移動して空いた部分は背景色を表示する
    - 移動速度はユーザが指定できるものとする
    - オプションとして、画像をわずかに拡大・縮小をゆっくり繰り返す追加エフェクトを付与できるものとする
    - 拡大は表示中画像サイズから最大5%、縮小は表示中ファイルの100%以下にならないようにする
    - 他のエフェクトと同等に「エフェクトを全カラムへ反映」ボタンで他のカラムへ反映できるようにする
- テキストリーダ機能を実装
    - カラムを跨ぐマスタウィンドウ全体に対するテキスト表示ウィンドウを表示し、ユーザが任意に読み込んだテキストファイルを表示する機能。
    - テキスト表示仕様
        - テキストは時間をかけて1文字ずつ表示する仕様とする。これは「テキストエフェクト」機能の描画速度と同じ仕様で実装する。
    - 設定項目
        - テキストウィンドウ表示位置：上下左右詰めの4種類
        - テキスト表示方向：縦書きと横書き
        - 背景不透明度スライダー: 0〜100%でウィンドウ背景を調整可能に（デフォルト70%＝透過度30%）
        - フォント：ドロップダウン、PCにインストールされたフォントをリストアップ
        - フォントサイズ：ドロップダウン、20 / 28 / 36px の3段階
        - テキスト描画速度：テキストボックス
        - ページ送り速度：遅い・通常・早い
    - UI要件
        - ウィンドウとフォントの可視性は、RPGゲームやRPGツクールで制作されたゲームのような視認性とする
        - テキストリーダの基本機能はUIに収め、操作に関連するUIはテキストウィンドウ上の任意の箇所に表示する（テキスト操作UI）
        - テキスト操作UIでは次の機能を用意する：ページ前後送りボタン、自動ページ送り開始終了ボタン、ページ送り速度調整(x2, x3)、ログ表示
        - ログ表示は画面全体にテキストウィンドウを広げ、前後のテキストを確認できる機能
    - スクリプト取り扱い要件
        - ユーザが用意した.txtファイルのみを読み込み可能な機能の実装
        - .txtファイル内で改行コードが2回連続した場合は分離したページとして解釈する。3回以上繰り返される改行コードは無視する
        - ページわけされずにウィンドウに描画しきれないと判断した場合は自動でページを分割する→表示中フォントサイズとサイズから計算可能か？
    - ショートカット操作
        - テキストウィンドウ上でホイールスクロールするとページを移動する
        - zまたはnで前ページへ移動、xまたはmで次ページへ移動
- 機能追加：テキストリーダー画像同期機能
    - 読み込んだ.txtファイル中に特定のタグを仕込み、テキストウィンドウ表示位置がタグ位置まで進んだタイミングでグリッドに画像を表示する機能
    - 1x1以上のカラムがある場合はすべて同じ画像を表示するものとする（プロトタイプ）
    - 画像の他に適用するエフェクトも指定できるものとする
    - プロファイルのjson形式を流用するのがいいと思うが、処理しやすいタグを検討してください
        - タグは2種類を用意する
        - 1. 簡易版：ユーザがテキストに直接「開始タグ＋画像パス」を記載することで画像だけを切り替えるタグ
        - 2. 通常版：本アプリを使ってタグを生成し埋め込むための複雑なタグ。画像／適用エフェクトなど詳細に指定できる。後述の「ストーリーボード作成機能」で編集できるものとする
            - 今後の破壊的変更に耐えられるように、アプリバージョンも保存する。
    - テキストウィンドウのページ進行にあわせてエフェクトを徐々に適用する機能
        - エフェクト適用率はタイマー同期と同じ仕様とし、「xページ進行で適用率x％にする」ような仕様で検討
    - タイマー再生機能
        - タイマータグが埋め込まれている場合はそのタグ表示時にタイマーを自動開始する
        - タイマー再生中はタイマーが完了するまでテキストリーダーの「Auto」は自動停止する
            - 手動でAutoを押し直せば再開できる
- 「ストーリーボード作成機能」の実装
    - テキストタブに機能の起動ボタンを設置
    - 機能起動時に作業用ファイルをローカル作成し、リアルタイムで反映させる（オートセーブ）
    - 小さめのフロートウィンドウを表示し、以下のメニューを表示
        - 「画像をここに差し込む」：テキストウィンドウで表示中のテキストの1行上にタグを仕込む
            - このボタンを押下した時点で表示されている画像と適用中のエフェクトすべてを復元できるタグをテキストファイルに挿入する
            - 「エフェクトを徐々に適用」オプション：ページの進行率に同期してエフェクトを適用する。 
                - 「xページで適用完了」入力値を用意し、そこで指定された値に近づくにつれてエフェクト適用率を上昇させる。
                - 指定値になったタイミングで100%とする。
        - 「タイマーをここに差し込む」：現在のアプリで設定中のタイマー設定をテキストウィンドウで表示中のテキストの1行上にタグを仕込む
            - この機能は前項の「エフェクトを徐々に適用」と競合しそうなので、優先度は低くする
        - 「ファイルを保存」：ボタン押下時に「{読み込み中ファイル名}+_WhiteRoom_+{yyyymmdd-hhmmss}+{拡張子}」として保存する
        - それぞれのボタンにマウスオーバーした際に、上記を要約した内容をTooltipとして表示する
        - ウィンドウの下部に小さい文字で次のTipsを表示する：「テキストファイルに空行を挿入すると改ページできます。」
    - 上記のタグ適用ボタン押下時に、すでに同位置にタグがある場合は上書きする
    - 現在の表示中のテキストがアプリによって自動的に改行された位置の場合は、実際の読み込み中ファイルの行に従ってタグを挿入する

### TODO
次のエフェクトの実装を検討：
- シェイクエフェクト：上下に揺れるエフェクト。ブリージングエフェクトの下に追加。
    - 表示中の画像を次の段階に従って動かす。
    - A. 単発方式
        - 1. ゆっくり画像を上に移動（持ち上げるような速度）・初期位置から上に40px
        - 2. 少し早く画像を下に移動（物が落ちるような速度）、初期位置から下に20px、以降はすべて同じ速度で移動
        - 3. (1)の半分の高さ位置まで移動（跳ね上がるような速度）
        - 4. (2)の半分の低さ位置まで移動
        - 5. これを繰り返し、3回バウンドして終了（初期位置に戻る）
        - 移動距離はこの初期値で実装し、ユーザが任意の係数をかけて調整できるようにする。
        - 移動速度も同様に調整可能とする。
    - B. 永久方式
        - 常に等速でユーザが指定した幅上下に移動を繰り返す。
        - 移動速度も選択可能とする
    - 移動は等速でなくイージングをかける。初回上昇時と落下時はeaseInCubic、バウンド時はeaseOutExpoで実装を検討して
- フラッシュエフェクト：点滅するエフェクト・任意の画像選択可。シェイクエフェクトの下に追加。
    - 
- スパイラルエフェクト：中心から広がる渦巻きのエフェクト


連日のご連絡大変失礼します…！

制作中のWhiteRoomにて、画像とテキストを同期する「ストーリーボード機能」というものを作成しました。
これをコミュニティの皆様がお試しできるサンプルを作成したく考えております。

そこで大変不躾なご相談で恐縮ですが、もし可能であればサンプルに使用させていただけるシナリオスクリプトがあれば、ぜひ使用させていただけないでしょうか。
もちろん新しく制作いただく必要はなく、すでにどこかで公開しているもので良いものがあればで問題ございません。

サンプル内容としてはRPGツクールの一連の会話シーンのようなもので短～中編程度を想定しており
シェイクエフェクト（揺らし誘惑など）とフラッシュエフェクト（フィニッシュや投げキッスなど）が使えるシーンが含まれているとなおありがたく存じます…！（※なくても大丈夫です）

サンプルストーリーボードは私自身が作成させていただく予定で、画像はAIイラストで生成する想定をしています。
AIイラストに使うキャラクターイメージは事前にご指示いただいても構いませんし、問題なければ私の方でイメージを作成します。
公開前に一度キャラクターイメージ画像、シーンごとの画像、最後に草稿のデモをお渡しいたしますので、
イメージにそぐわない場合は何度でもリテイクをお出しいただいて構いません。

シナリオや各種ドキュメントなどにはけしのみ先生のお名前、アカウント、URLなどのクレジットを入れさせていただきます。

また、このサンプルは将来的にアプリのサンプルとして無期限に公開し続ける可能性があります。
もし途中で取り下げのご希望がございましたら、その時は即時作成したサンプルは取り下げさせていただきます。

以上ご確認いただけますと幸いです…！
