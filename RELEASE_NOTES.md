# Release Notes

## v1.5.12

### 機能追加

- エフェクトユーザープリセット保存・編集・削除・適用機能を追加（プリセット適用ボタン、インライン名前編集、長押し削除、「+ 現在の設定を保存」ボタン）
- ユーザーデフォルト設定の自動保存・自動読み込み機能を追加（`userData/whiteroom-defaults.json` に保存、起動時に自動適用）
- Ctrl+S でデフォルト設定保存ショートカットを追加し、保存通知を画面中央上に表示
- 右ドラッグ横方向で UI 表示・非表示を切り替えるショートカットを追加
- グリッド上の右クリックドラッグでフルスクリーン切替ショートカットを追加（上ドラッグ: フルスクリーン、下ドラッグ: ウィンドウ）

### 調整

- エフェクトタブの UI 順序を整理し、プリセットセクションを新設（「すべてのカラムに反映する」を最上部、サンプルプリセットをプリセットセクションに移動）
- スタッシュ上限を 30 に変更、プロファイル読み込み・スタッシュ復元時に先頭グリッドを自動選択
- サンプルプリセットボタンのラベルから「サンプルプリセット - 」プレフィックスを削除

### バグフィックス

- デフォルト設定保存時にスタッシュ情報を含めるよう修正
- スタッシュフロートが画面高さの 80% を超えたときにスクロール可能になるよう修正

## v1.5.11

### 機能追加

- アセット・テキストエフェクトに表示エリア指定を追加（矩形エリアの選択 UI と描画、アセットは開始抽選のみに適用してエリア外も表示を維持）
- 2段階追従エリアの位置調整と左右反転を追加（親エリア内で2段目を上下左右に移動、複製 ON 時は左右反転で2円が近づく/離れる挙動）

### 調整

- テキストエフェクトを表示間隔に従い同時表示するよう変更（前の表示完了を待たず intervalMs ごとに新規テキストを開始し、複数のエフェクトが重なって表示）

## v1.5.10

### 機能追加

- シェイクエフェクトにアニメーション方向を10度刻みで調整できる方向オプションを追加

## v1.5.9

### 機能追加

- フラッシュエフェクトに画像フォルダからランダムに描画する表示ファイルモードを追加

### 調整

- フラッシュエフェクトの表示ファイル UI から重複していたフォルダランダム描画ボタンを削除

### バグフィックス

- フラッシュエフェクトの画像フォルダランダム描画時に前回画像が一瞬表示される問題を修正

## v1.5.8

### 機能追加

- `.sut` ブラシ素材のラスタ解決を追加し、複数ティップのランダム利用と先頭ティップのみ利用を選択可能に
- アセットエフェクトに波紋パターン、移動パターン選択、Bounce 付加効果、ラスタ色反転、非表示エリア指定を追加
- フォーカスエフェクトと検閲エフェクトを追加し、フォーカス連動・シェイクエリア連動・繰り返しテキスト表示に対応
- シェイクエフェクトのトレイルエリアを最大 5 箇所まで複数指定できるように拡張

### 調整

- アセット対応形式、プリセット表示名、色適用、HoverTooltip、Row 配置、README 表記を整理
- 波紋パターンの周辺制御を spawn 位置ベースへ変更し、非表示エリアスライダーを常時表示に調整
- フォーカスブラー方式のラベル、検閲エフェクトの既定値、シェイクエリア追加時の複製挙動と UI ラベルを調整
- フォーカス・検閲・シェイク・SUT ブラシ関連の仕様書を追加・更新

### バグフィックス

- マルチバイト文字を含むパスの画像ファイルを読み込めない問題を修正
- フォーカスエフェクトのマスク、レイヤー順、ブラー範囲、ポイント再指定時クラッシュを修正
- 検閲エフェクトのテキスト表示、フォーカス連動切替時のマスク破棄、シェイクエリア描画を修正
- シェイクトレイルのピックモード中ビジュアライザ追従、エリア追加ボタン下余白、フォーカス時ブラー適用を修正

## v1.5.7

### 機能追加

- 起動スプラッシュ画面を追加（静的表示とフェードアウト）
- フラッシュに放射状フェード（周囲透明強度・不透明範囲）とブラー強度スライダーを追加
- フラッシュに範囲選択アセット、範囲選択中の画像カバー表示、ベクター選択時のアセット色・色透明度を追加
- アセットに付加効果（Wiggle 等）、縁ぼかし、上昇速度調整、ランダムサイズ・色適用度ランダムを追加
- 追従遅延に円の複製・横縦間隔・上下交互オフセットを追加
- 選択セルに黄枠ビジュアライザを追加（フェードで完全非表示）

### 調整

- フラッシュからスライド遷移を廃止し、ズーム開始を 0.5s フェードインに変更；維持時間 UI と表示フェーズ下限を簡素化
- フラッシュのベクターアセット・拡大率と高解像度描画、表示ファイルモード、`colorOverlayEnabled` 整理、周囲透明のシェーダー係数を調整
- アセット／フラッシュの表示ファイル UI とプリセット既定を整理；プリセット画像 `heart-sketch` を削除しビルド・文言を追随
- 各エフェクトセクションのタイマー同期トグルを末尾へ移動；全カラム反映ラベルを短縮し選択中画像の一斉表示に対応
- タイマータブのセクションラベルを「特殊制御」に変更；README にスタッシュ説明とエフェクト一覧テーブル（日英）を追記・整理
- スタッシュ残存時の終了確認を専用ダイアログ化し文言とサイズを調整
- 非表示時フロート UI の右端トリガー幅を拡大；追従遅延の第 2 段ガイドを青円中心にし円サイズ最小を 5% に

### バグフィックス

- パッケージ起動時にスプラッシュのフェードが効かない問題を修正
- フラッシュ終了ズームの開始スケールを 1 に連続化；放射状フェードの中心座標ずれを修正
- タイマー表示を合計時間基準に戻し、詳細制御の自動次画像・同期進捗・途中開始クランプを修正
- 円の間隔変更時に追従遅延の青黄ガイドを同時表示するよう修正
- その他メニューのホバークリック、エフェクトカテゴリ間の余白、Wiggle の移動・回転基点などを修正

## v1.5.6

### 機能追加

- スタッシュ機能を追加（`AppProfile` v1.1.0、フロート UI、エクスポート・インポート、終了時の残存警告）
- 画像グリッドの右長押しでスタッシュを開く
- カラーフィルタに動的反映とタイマー同期を追加（ビネットと同仕様）
- トーンフィルタに輝度スライダーを追加
- ビネット強度スライダーを追加し、透明度 UI を Row に統一
- 動的アセットにベクターハート（builtin）を追加しラスタと併存

### 調整

- スタッシュ UI を改善（長押しで取り出し・削除、プログレス表示、ホバーで縮小アイコン展開、開閉アニメ、パネル幅など）
- スタッシュを `[s]` で最後のマウス位置付近に開く仕様に変更し、ハンバーガーメニューからスタッシュ項目を削除
- ハンバーガーメニューとその他タブをホバーで開閉する挙動に変更（テキスト窓との重なり時の透過など）
- ブランクグリッドのショートカット Tips と空セル Tips のレイアウト・表記を調整
- ベクターハートの既定サイズとアセット選択ラベルを調整し、Vector Stamp ドラフト仕様とドキュメントを追加・更新

### バグフィックス

- スタッシュ POP 後に画像別エフェクト JSON をキャッシュ再読み込みするよう修正
- エコー効果を normal 合成に変更し実装を簡素化
- スクイッシュ／ズームのモード判定で発生していた TypeScript の型エラーを解消

## v1.5.5

### 機能追加

- フォグエフェクトを実装し、重なり発生・蒸発アニメーションに対応
- ズームエフェクトを追加し、スクイッシュとの相互同期機能を実装
- スクイッシュエフェクトを拡張し、永久アニメーション方式・位置ランダム・放射リング付加効果・円間隔補正オプションを追加
- ズーム／スクイッシュ／フォグ／シェイクにタイマー同期設定を追加

### 調整

- エフェクトタブにカテゴリ分類 UI を追加し、セクション構成と表示ラベルを整理
- フォグ、ズーム、スクイッシュの既定値とタイマー同期 UI の挙動を調整
- ピックモード中の Esc キー挙動を見直し、フルスクリーン維持時の操作性を改善
- README と開発ルールを更新

### バグフィックス

- エコーの視認性と放射状ブラー併用時の描画順不整合を修正
- ズーム併用時のシェイク追従遅延レイヤーの座標・スケール同期不具合を修正
- リサイズ時のフィルタ再描画とトーン適用範囲の不整合を修正
- フロート UI 再表示時のスクロール位置リセット、プリセット適用時のガイド誤表示を修正

## v1.5.4

### 機能追加

- ストーリーボードタグにタイマーの途中開始・途中終了（partial）設定の保存・復元を追加
- スクイッシュエフェクトを追加し、有機的オプション・中心色取得・カラーピックに対応
- ストーリーボード読み込み時の確認ダイアログと排他制御を追加（操作制限解除ボタン含む）
- プロファイルにウィンドウサイズ・UI表示状態を保存し、`.json` / `.txt` の D&D 読み込みに対応

### 調整

- ストーリーボード画像タグとタイマー設定を統合し、タグ保存フローを簡素化
- 外観タブの言語表示ラベルや確認ダイアログ文言など UI 表示を調整
- Windows 配布ファイル名などリリース周辺設定を更新

### バグフィックス

- ストーリーボードタグのタイマー設定保存・復元漏れと自動開始不具合を修正
- タグ挿入位置と複数タグ連続時の適用画像選択ロジックを修正
- 放射状ブラーのリサイズ追従を修正
- プリセットアセット参照のポータブル環境互換を修正
- セルナビゲーションボタン付近でフロート UI が干渉する問題を修正

## v1.5.3

### 機能追加

- ストーリーボードに読書設定の保存・復元機能を追加
- タイマー終了時に次の画像へ自動遷移するオプションを追加
- 画像別エフェクト保存にタイマー設定を追加し、スライドショーとタイマーを連携
- シェイク追従遅延に「ベース画像を固定」オプションを追加

### 調整

- 自動遷移後にタイマーをリセット・再生する処理を追加
- グリッド・タイマーUIとタイマー連携メッセージを調整
- README と開発メモを更新

### バグフィックス

- スパイラルがウィンドウ端まで届かない問題を修正

## v1.5.2

### 機能追加

- 画像別エフェクトプロファイル機能を追加
- ピックモードの円高さ調整ショートカットを追加
- 画像ナビゲーションショートカットを追加
- ウィンドウサイズリセット機能を追加

### 調整

- 画像別エフェクトプロファイル仕様を追加
- エフェクト保存ボタンのラベルを変更
- シェイクエフェクトの初期値を調整
- 開発メモとプロジェクトルールを更新

### バグフィックス

- 円形範囲指定のエリアがウィンドウサイズ変更後も比率とサイズを維持するよう修正
- 中心位置指定をクリック解除時に適用するよう修正
- 中心位置選択モードのカラム判定を修正
- シェイク追従範囲の全カラム反映と描画順を修正
- シェイク追従2段目を前面表示に修正
- 画像遷移時のエフェクト再生成を軽量化
- 画像切替時の円形ガイド表示を抑制

## v1.5.1

### 機能追加

- 単発シェイクエフェクトの繰り返しオプションを追加
- エフェクト中心位置の指定操作を改善

### 調整

- 中心位置指定の操作ガイダンスを更新（ショートカット `[P]` と右ドラッグによるサイズ変更を追記）
- リリースバンプ手順のプロジェクトルールを追加

### バグフィックス

- 中心位置指定の凡例をローカライズ
- 円サイズビジュアライザーの説明を明確化

## v1.5.0

### 機能追加

- シェイクエフェクトを実装（残像トレイル・減衰揺れ対応）
- シェイクエフェクトに2段階追従オプションを追加（移動速度スライダー・単発動作確認ボタン）
- シェイクエフェクトの高度なトレイルコントロールを追加（ブラー強度・遅延上限設定）
- シェイク追従遅延の中心位置をクリックまたはボタンで調整可能に（ガイド線表示）
- フラッシュエフェクトを実装
- スパイラルエフェクトを実装（渦流モード・放射線状モード）
- ブラー・スパイラルセクションにエフェクト中心位置調整ボタンを追加

### 調整

- シェイク残像と減衰揺れのパラメータを調整
- スパイラルエフェクトの初期値・UIの操作性を調整
- 渦流スパイラルの回転速度係数を調整
- 中心位置指定ロジックを共通化しエフェクトUIを統合

### バグフィックス

- トレイル遅延の上限を0.5秒に制限

## v1.4.2

### 機能追加

- ストーリーボード画像として URL を指定できる機能を追加（GridタブおよびStoryboardパネル対応）
- ストーリーボードのリモート画像に通信制限を追加（pixiv 系ホストは1セッション10件まで）
- テキストウィンドウの最大幅を設定可能に

### 調整

- README と開発方針に pixiv 制限の説明を追記

### バグフィックス

- ストーリーボード画像表示中に空セルの Tips が表示される問題を修正
- pixiv 画像をオリジナル解像度で読み込むよう修正

## v1.4.1

### 機能追加

- テキストリーダー画像同期・ストーリーボード作成機能を追加
  - 簡易タグ `[[画像パス]]` とリッチタグ `[WR:1.5.0:{...}]` によるページ連動画像・エフェクト切り替え
  - ページ後退時のベースライン自動ロールバック
  - `progress` 指定時のエフェクト段階適用、`timer` 指定時のタイマー自動開始
  - ストーリーボードパネル：現在位置へのタグ差し込み・ファイル保存
- 選択カラムのエフェクト一括リセットを追加

### 調整

- テキストリーダーの表示方向と幅配置を改善
- テキストウィンドウの操作 UI をグループ化して整理
- ショートカット Tips を整理して UI 切替を追加
- 開発者情報と GitHub リンクをプロファイルタブへ移動

### バグフィックス

- テキストログのスクロールと一時ファイル保存を修正
- ストーリーボードパネルを UI 最前面に表示するよう修正
- D&D 画像読み込み後に古い `cellTagOverrides` が残り差し込み画像がズレる問題を修正
- ストーリーボードパネルをヘッダードラッグで任意位置に移動可能に
- 「エフェクトを徐々に適用」がタイマー同期以外のエフェクトに効かない問題を修正
- `applyTagToAllCells` 内の `structuredClone` が immer Proxy に対して DOMException を起こす問題を修正

## v1.4.0

### 機能追加

- UIタブのハンバーガーメニュー化：外観・プロファイルタブをタブバー右端の≡メニューに格納（ドロップダウン、メニュー外クリックで閉じる）
- UI非表示時に右端フロート表示を追加
- テキストリーダー：背景不透明度スライダーを追加（デフォルト70%、0〜100%）
- テキストリーダー：「ウィンドウを画像の上に表示」トグルを追加（OFFでキャンバスをウィンドウ分縮小）

### 調整

- 自動ページ送りボタンのラベルを「Auto」に統一し、有効時はハイライト表示に変更
- テキストウィンドウに12pxマージンを追加しエッジ密着を解消
- フォントサイズを20 / 28 / 36px の3段階に変更（旧: 20 / 40 / 60px）
- top / bottom 配置時のウィンドウ高さをフォントサイズ×3行分に自動調整
- テキストログウィンドウをウィンドウ全体に表示（マージン・縁取り削除）
- セクションラベルを「ウィンドウ設定」「フォント設定」「速度設定」に変更
- プリセットアセット未選択時のラベルを変更
- Electron 用アイコン設定を追加
- README に macOS インストール手順・Gatekeeper 回避方法を追加

### バグフィックス

- エフェクトタブのサンプルプリセットを多言語対応
- テキストウィンドウ・テキストログ上でフロートUIが誤表示される問題を修正（`data-reader-window` 属性でエリアを識別して抑制）
- テキスト速度 0ms 設定時に全文字を即時表示するよう修正

## v1.3.1

### 機能追加

- フルスクリーン移行時の UI 自動非表示／復元を追加
- タイマー終了直前オーバーレイ画像表示を追加
- タイマー同期エフェクト適用タイミングをタイマー終了の N 秒前に設定可能に
- タイマー終了時のリセット操作・フェードアウトを実装
- タイマー同期の全解除ボタンを追加
- スペースキーによるタイマー開始／停止に対応
- ホバー時に操作ボタンを表示する UI を追加
- ブランクセルに Tips 表示を追加
- プリセットアセット選択肢のビルド時埋め込みとパッケージ対応を実装

### 調整

- エフェクトオプションセクションを廃止し、タイマー同期セクションに統合

### バグフィックス

- D&D 時の既存画像上書き挙動を修正
- グリッドリセット時の不具合を修正

## v1.3.0

### 機能追加

- アセットエフェクト「発生」を追加
- エフェクトのタイマー同期機能を実装
- タイマー点滅機能を実装

### 調整

- その他 UI 調整

### バグフィックス

- その他バグフィックス

## v1.2.0

### 機能追加

- 色調オーバレイに画像強調フィルタ機能を追加
- 色調オーバレイの機能名称をカラーフィルタに変更
- ブラーエフェクトの放射線状ブラーに新方式（パターンB）を実装し、既存ブラー方式と切り替え可能に
- アセットエフェクトに「上昇」に加えて「発生」エフェクトを追加
- アセットエフェクト用のサンプルファイルを追加
- `\assets\image\heart` 配下からアセットエフェクト画像を選択できる機能を追加
- 画像を手動で移動できる機能を実装し、ホイールスクロール操作にも対応

## v1.1.0

### 機能追加

- 初期正式版
- 色調オーバレイを追加
- ビネットエフェクトを追加
- ブラーエフェクトを追加
- エコーエフェクトを追加
- ブリージングエフェクトを追加
- テキストエフェクトを追加
- アセットエフェクトを追加
